# Phase 2 Additional Dependencies
# ==========================================
# Install these ON TOP OF Phase 1 requirements

# ============================================================================
# Slack Interface
# ============================================================================
slack-bolt==1.18.0  # Official Slack SDK for Python

# ============================================================================
# REST API (uses existing FastAPI from Phase 1)
# ============================================================================
# No additional dependencies - uses existing:
# - fastapi
# - uvicorn
# - pydantic

# ============================================================================
# Persistent Memory (uses existing dependencies)
# ============================================================================
# No additional dependencies - uses existing:
# - aiosqlite
# - cryptography

# ============================================================================
# Cloud LLM Backends (uses existing aiohttp)
# ============================================================================
# No additional dependencies - uses existing:
# - aiohttp

# Optional: If you want official SDKs instead of direct API calls
# openai==1.6.1  # Official OpenAI SDK
# anthropic==0.8.1  # Official Anthropic SDK

# ============================================================================
# Voice Support (Web) - No Python dependencies
# ============================================================================
# Uses browser Web Audio API (JavaScript only)
# No server-side dependencies needed

# ============================================================================
# Summary
# ============================================================================
# Required for Phase 2: Only slack-bolt if using Slack
# Everything else reuses Phase 1 dependencies!

# Install command:
#   pip install slack-bolt==1.18.0
